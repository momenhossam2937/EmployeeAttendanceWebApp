using System;
using System.Collections.Generic;
using System.Text;

namespace EmployeeAttendanceWebApp.Application.Common.Constants
{
    public static class Constants
    {
       
        public const string EMAIL_PATTERN = @"(?=(?:^\w))\w+([-+.]\w+)*@\w+([-.]\w+)*\.\w+([-.]\w+)*(?<=[^ ])$";
    }
}
